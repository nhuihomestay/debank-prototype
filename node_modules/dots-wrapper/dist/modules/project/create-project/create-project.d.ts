import { IResponse, IContext } from '../../../types';
import { IProject } from '..';
export interface ICreateProjectApiResponse {
    project: IProject;
}
export interface ICreateProjectApiRequest {
    name: string;
    description: string;
    purpose: string;
    environment: string;
}
export declare type CreateProjectResponse = IResponse<ICreateProjectApiResponse>;
export declare const createProject: ({ httpClient, }: IContext) => ({ name, description, purpose, environment, }: ICreateProjectApiRequest) => Promise<Readonly<CreateProjectResponse>>;
