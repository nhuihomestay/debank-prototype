import { IResponse, IContext, IListResponse, IListRequest } from '../../../types';
import { IApp } from '..';
export interface IListAppsApiResponse extends IListResponse {
    apps: IApp[];
}
export declare type ListAppsResponse = IResponse<IListAppsApiResponse>;
export declare const listApps: ({ httpClient, }: IContext) => ({ page, per_page, }: IListRequest) => Promise<Readonly<ListAppsResponse>>;
