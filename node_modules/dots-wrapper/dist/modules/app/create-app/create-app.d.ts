import { IResponse, IContext } from '../../../types';
import { IApp, IAppSpec } from '..';
export interface ICreateAppApiResponse {
    app: IApp;
}
export interface ICreateAppApiRequest {
    spec: Partial<IAppSpec>;
}
export declare type CreateAppResponse = IResponse<ICreateAppApiResponse>;
export declare const createApp: ({ httpClient, }: IContext) => ({ spec, }: ICreateAppApiRequest) => Promise<Readonly<CreateAppResponse>>;
